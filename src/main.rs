use ::rand::Rng;

fn main() {
    const VLAN: &str = "Institutionalized discrimination";
    const DNS: &str = "Always works great for me";
    const STATIC: &str = "My network never goes down because I use ... routing";
    const BRNO: &str = "MRDAT";
    let msg: String = "meow ;3".to_string();

    println!("2dcqb!");
    let value = rand::thread_rng().gen_range(0..=5);

    while true {
        println!("{msg}");
    }
}
